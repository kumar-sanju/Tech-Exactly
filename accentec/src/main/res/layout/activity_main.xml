<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/textViewTV"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="The PDF is saved in your device as according to our cuttent time stamp"
        android:textAlignment="center"
        />

    <Button
        android:id="@+id/selectImagesButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/textViewTV"
        android:text="Select Images" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/imageRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="10dp"
        android:layout_below="@+id/selectImagesButton"
        android:layout_above="@+id/textView"
        />

    <TextView
        android:id="@+id/textView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Here is your PDF file Path"
        android:textAlignment="center"
        android:layout_above="@+id/convertToPdfButton"
        />

    <Button
        android:id="@+id/convertToPdfButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Convert to PDF"
        android:layout_alignParentBottom="true"
        android:enabled="false" />

</RelativeLayout>
